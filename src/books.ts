export type BookSource = {
  author: string;
  createdAt: number;
  fileName: string;
  fingerprint: string;
  id: string;
  size: number;
  storageKey: string;
  title: string;
  updatedAt: number;
};
